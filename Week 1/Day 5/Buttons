#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios:: sync_with_stdio(false);
    cin.tie(nullptr);
    
    int a,b;
    cin >> a >> b;

    if(a<b)
    {
        swap(a,b);
    }
    int maximum = max(2*a - 1 , a+b);
    cout << maximum << endl;
    
    return 0;
}
