#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int a, b, result;
    cin >> a >> b;
    if (a < b)
    {
        result = (b - a) + 1;
        cout << result << endl;
    }
    else
        cout << 0 << endl;

    return 0;
}
